export const profs = (state) => state.professors;

export const selectedProf = (state) => state.selectedProf;

export const profsCount = (state) => state.professors.length;

export const promotions = (state) => state.promotions;

// COURSE

export const courses = (state) => state.courses;

export const selectedCourse = (state) => state.selectedCourse;

// JURY

export const jury = (state) => state.jury;
